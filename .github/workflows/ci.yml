name: ci

# Temporary proof pin for BelimbingApp/belimbing#662 composed subject-id scan.
# Not for merge — close after the domain-ci step is visible on a green run.

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  ci:
    uses: BelimbingApp/belimbing/.github/workflows/domain-ci.yml@839ee6a4363a2c4fc3f642c56016ffffc1c6ac10
    with:
      domain-path: app/Domains/People
      platform-ref: 839ee6a4363a2c4fc3f642c56016ffffc1c6ac10
      sonar-project-key: BelimbingApp_blb-people
      sonar-organization: belimbingapp
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
